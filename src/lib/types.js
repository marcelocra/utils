/**
 * Import this file for its effects: it defines all types, using TypeScript syntax.
 *
 * @typedef {string[]} Keys
 * @typedef {(string|number)[]} Values
 * @typedef {Error | Iterable<[string, string|number]>} ZipReturn
 */
